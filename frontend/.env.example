# API origin (Express). For local dev with Vite proxy you can leave empty and use relative /api paths.
VITE_API_URL=http://localhost:4000

# Optional: override Vite dev proxy target (see vite.config.js)
# VITE_PROXY_TARGET=http://localhost:4000
